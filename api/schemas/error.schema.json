{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Error",
  "description": "What the API returns when a call is refused. Worth a schema of its own: an error path that answers with an empty body, or with HTML from a proxy, is a real failure and one that is easy to miss when only the status code is asserted.",
  "type": "object",
  "required": ["message"],
  "properties": {
    "message": { "type": "string", "minLength": 1 }
  }
}
