# Virtual environment
venv/

# Python cache files
__pycache__/
*.pyc

# Pytest cache
.pytest_cache/

# OS junk
.DS_Store

# Scraper output
scraped_books.csv

# Secrets - never commit
.env

# Test reports, and the failure artifacts that --output now nests inside them.
# Kept last: the two entries under it are only for runs that bypass pytest.ini
# and fall back to Playwright's default locations.
reports/
test-results/
playwright-report/